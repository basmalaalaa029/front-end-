# ── Core ─────────────────────────────────────────────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0
langgraph>=0.2
python-dotenv>=1.0

# ── LLM / Model Inference ────────────────────────────────────────────────────
transformers>=4.40
torch>=2.0
safetensors>=0.4
tokenizers>=0.15
peft>=0.10
accelerate>=0.25
huggingface-hub[cli]>=0.20
sentencepiece>=0.1          # tokenizer fallback for some models
bitsandbytes>=0.41          # 4-bit quantization (GPU only)

# ── RAG & Embeddings ─────────────────────────────────────────────────────────
sentence-transformers>=2.2
faiss-cpu>=1.7
scikit-learn>=1.3

# ── API Server ────────────────────────────────────────────────────────────────
fastapi>=0.100
uvicorn[standard]>=0.25
python-multipart>=0.0.9     # required for file uploads
PyJWT>=2.8                  # JWT verification (same secret as backend)
gunicorn>=21.0              # production process manager

# ── PDF ───────────────────────────────────────────────────────────────────────
pdfplumber>=0.10
pypdf>=4.0
reportlab>=4.0
mistune>=3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
PyYAML>=6.0
numpy>=1.24
httpx>=0.27                 # HTTP client (optional integrations)
modal>=0.64.0               # poll Modal GPU analysis results via FunctionCall
requests>=2.28               # HTTP client for job board APIs
python-docx>=0.8.11          # DOCX CV reading
google-generativeai>=0.8     # Gemini CV enhancement

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0

# ── Career Interview (LangGraph + Groq) ───────────────────────────────────────
langchain-core>=0.3.0
langchain-groq>=0.2.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
pdfminer.six>=20221105
ffmpeg-python>=0.2.0
openai-whisper>=20231117
librosa>=0.10.0
scipy>=1.11.0
opencv-python-headless>=4.8.0
imageio-ffmpeg>=0.5.1
piper-tts
