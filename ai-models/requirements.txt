# ── Core ─────────────────────────────────────────────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0
langgraph>=0.2
python-dotenv>=1.0

# ── LLM / Model Inference ────────────────────────────────────────────────────
transformers>=4.40
torch>=2.0
safetensors>=0.4
tokenizers>=0.15
peft>=0.10
accelerate>=0.25
huggingface-hub[cli]>=0.20
sentencepiece>=0.1          # tokenizer fallback for some models
bitsandbytes>=0.41          # 4-bit quantization (GPU only)
llama-cpp-python>=0.3       # fast CPU inference via quantized GGUF models

# ── RAG & Embeddings ─────────────────────────────────────────────────────────
sentence-transformers>=2.2
faiss-cpu>=1.7
scikit-learn>=1.3

# ── API Server ────────────────────────────────────────────────────────────────
fastapi>=0.100
uvicorn[standard]>=0.25
python-multipart>=0.0.9     # required for file uploads
PyJWT>=2.8                  # JWT verification (same secret as backend)
gunicorn>=21.0              # production process manager

# ── PDF ───────────────────────────────────────────────────────────────────────
pdfplumber>=0.10
pypdf>=4.0
reportlab>=4.0
mistune>=3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
PyYAML>=6.0
numpy>=1.24
sse-starlette>=2.0            # llama_cpp.server fallback when native llama-server missing
httpx>=0.27                 # llama-server HTTP client for cv_analysis judges
requests>=2.28               # HTTP client for job board APIs
python-docx>=0.8.11          # DOCX CV reading
google-generativeai>=0.8     # Gemini CV enhancement

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0
