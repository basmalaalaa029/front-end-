# CareerPilot AI models — production env (set in Render dashboard)
# JWT_SECRET must match backend/.env

PORT=8000
HOST=0.0.0.0
RELOAD=false
LOG_LEVEL=info

JWT_SECRET=generate-a-strong-random-secret
CV_AGENT_AUTH_DISABLED=false
ALLOWED_ORIGINS=https://your-app.vercel.app
FRONTEND_URL=https://your-app.vercel.app

# API-only mode — no local GPU models on Render
ALLOW_MODEL_DOWNLOAD=false
CV_WARMUP_MODEL=false
USE_PHI3_WRITER=false
MODEL_PRELOAD_MODE=none

# Modal CV analysis
MODAL_PROFILE=essamalaa0355
MODAL_ENDPOINT_URL=https://essamalaa0355--cv-analysis-cvanalysismodel-analyze.modal.run

# External APIs
HUGGINGFACE_TOKEN=
GEMINI_API_KEY=
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
