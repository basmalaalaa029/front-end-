# Job Agent UI kit

Autonomous job search — ranked matches with agent activity drawer.

**Components**
- `JobCard` — company logo, title, meta row, AI "why we matched you" line, score pill
- `AgentDrawer` — live task list (done/active/pending) with pulsing indicator
- `ContextPanel` — right-rail with match breakdown + Apply CTA
