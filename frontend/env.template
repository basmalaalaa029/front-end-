# CareerForge frontend — copy to .env: cp env.template .env
# Only VITE_* variables are exposed to the browser.

VITE_API_URL=http://localhost:5000/api
VITE_CV_AGENT_URL=http://localhost:8000

# OAuth (optional — uncomment when OAuth is configured in backend/.env)
# VITE_OAUTH_CALLBACK_URL=http://localhost:5173/auth/callback
# VITE_OAUTH_GOOGLE_URL=http://localhost:5000/api/auth/google
# VITE_OAUTH_LINKEDIN_URL=http://localhost:5000/api/auth/linkedin
